# GPU overlay — apply on hosts with an NVIDIA GPU + nvidia-container-toolkit:
#   docker compose -f docker-compose.prod.yml -f docker-compose.gpu.yml up -d
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
